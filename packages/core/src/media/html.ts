export const load = (
  source: string,
  mediaElement: HTMLMediaElement
) => {
  const htmlMedia = mediaElement.load()

  return htmlMedia
}

export const play = () => {

}

export const pause = () => {

}